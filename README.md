# dspy-optimization
This Repository gives an Introduction to Dspy. The repo has all examples that explains the importance of using Dspy in regular projects. I will show it with and without using Multi-Agent Frameworks

# Enable ML-Flow for local debugging

- Run the following command
```sh
mlflow server --backend-store-uri sqlite:///mydb.sqlite
```